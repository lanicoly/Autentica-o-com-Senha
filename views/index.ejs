<!DOCTYPE html>
<html lang="pt-Br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <title>Prog. WEB II</title>

    <style>
        body {
            background-color: rgb(226, 227, 241);
            font-family: 'inter';
        }

        section {
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .post {
            width: 30%;
            margin: 10px;
        }
        .formulario {
            background-color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            width: max-content;
            height: auto;
            border: solid 1.5px rgb(202, 202, 202);
            padding: 10px;
            border-radius: 20px;
            margin: 20px;
        }

        form {
            width: 100%;
            height: 100%;
            margin: 10px 30px;
        }

        form span {
            margin: 10px 0px;
            text-align: left;
            font-size: 14px;
        }

        .campo-form {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 4px;
        }

        .itens-forms {
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        label{
            font-size: 12px;
        }
        
        input {
            flex:1;
            padding: 6px;
            border: solid 1.5px rgb(202, 202, 202);
            border-radius: 4px;
        }

        .botoes {
            display: flex;
            justify-content: space-between;
            gap: 10px;
        }

        button {
            padding: 6px 12px;
            border-radius: 6px;
            background-color: rgb(10, 69, 147);
            color: white;
            font-size: 14px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            margin: 8px 0px;
            display: flex;
            gap: 6px;
            justify-content: center;
            align-items: center;
        }

        button:hover {
            transform: scale(1.06);
            transition: all .2s ease-in-out;
            background-color: rgb(32, 143, 212);
        }

        button i {
            font-size: 20px;
        }

        
        .tabela {
            display: flex;
            justify-content: center;
            margin: 10px;
            width: 100%;
        }

        table {
            min-width: 80%;
            border: none;
            border-spacing: 4px;
        }

        th {
            padding: 10px 20px;
            margin: 10px;
            background-color: rgb(10, 69, 147);
            font-size: 16px;
            color: white;
            width: auto;
        }

        table tr td{
            background-color: white;
            padding: 4px 8px;
            text-align: center;
            width: auto;
            flex-wrap: wrap;
        }

        .botao-excluir {
            background-color: rgb(255, 223, 223);
            border: solid 3px brown;
            color: brown;
            font-size: 14px;
            padding: 4px 8px;
        }

        .botao-excluir:hover {
            background-color: brown;
            border: none;
            color: white;
            padding: 7px 11px;
            transform: scale(1);
            transition: none;
        }

        .conteudo-central {
            display: flex;
            gap: 16px;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
        }

        @media screen and (max-width: 880px) {
            .post{
                width: 90%;
            }

            .formulario {
                width: auto;
            }
        }

    </style>

<body>
    <div class="conteudo-central">

    <section class="get">
        
        <div class="tabela">
            <table>
                <thead id="topo">
                    <tr>
                        <th style="border-top-left-radius: 10px;">ID</th>
                        <th>Nome</th>
                        <th>E-mail</th>
                        <th style="border-top-right-radius: 10px;">Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% usuarios.forEach(usuario=>{ %>
                        <tr>
                            <td><%= usuario.id %></td>
                            <td><%= usuario.nome %></td>
                            <td><%= usuario.email %></td>
                            <td>
                                <button class="botao-excluir" onclick="window.location.href='/excluir/<%= usuario.id %>'">
                                    Excluir
                                </button>
                            </td>
                            <%}) %>
                        </tr>
                    </tbody>
            </table>
        </div>

        <a href="/logout"><i class="bi bi-arrow-left"></i>Sair</a>
        
    </section>
</div>


</body>

</html>